// Stub used by Vitest to satisfy require()/import of binary asset files
// (jpg / png) that are bundled by Metro at runtime but cannot be evaluated
// as JavaScript inside the Vitest Node environment.
export default 0
module.exports = 0
