rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Deny all — app does not use Cloud Storage in V1
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
